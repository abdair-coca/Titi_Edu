import { useCallback, useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import TitiMascot from '../../components/TitiMascot.jsx';
import { authoringError, authoringMutation } from '../../lib/authoring.js';
import client from '../../api/client.js';

const SCOPES = ['course:read', 'course:create', 'content:write', 'material:write', 'publish', 'analytics:read'];

export default function Integrations() {
  const navigate = useNavigate();
  const [tokens, setTokens] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(null); const [busy, setBusy] = useState(false); const [reveal, setReveal] = useState(null);
  const [name, setName] = useState(''); const [expiresInHours, setExpiresInHours] = useState(720); const [scopes, setScopes] = useState(['course:read']);
  const load = useCallback(async () => { setLoading(true); setError(null); try { const { data } = await client.get('/api/authoring/service-tokens'); if (!data?.success) throw new Error(data?.message || 'No se pudieron cargar los tokens'); setTokens(data.data.tokens || []); } catch (err) { setError(authoringError(err, 'No se pudieron cargar los tokens')); } finally { setLoading(false); } }, []);
  useEffect(() => { load(); }, [load]);
  function toggleScope(scope) { setScopes((current) => current.includes(scope) ? current.filter((item) => item !== scope) : [...current, scope]); }
  async function create(event) { event.preventDefault(); if (!name.trim() || !scopes.length) { setError('Indicá nombre y al menos un permiso'); return; } setBusy(true); setError(null); try { const { data } = await authoringMutation('post', '/service-tokens', { nombre: name.trim(), expiresInHours: Number(expiresInHours), scopes }); if (!data?.success) throw new Error(data?.message || 'No se pudo crear el token'); setReveal(data.data.token); setName(''); await load(); } catch (err) { setError(authoringError(err, 'No se pudo crear el token')); } finally { setBusy(false); } }
  async function revoke(token) { setBusy(true); setError(null); try { const { data } = await authoringMutation('post', `/service-tokens/${token.id}/revoke`, {}); if (!data?.success) throw new Error(data?.message || 'No se pudo revocar'); await load(); } catch (err) { setError(authoringError(err, 'No se pudo revocar')); } finally { setBusy(false); } }
  async function copy() { try { await navigator.clipboard.writeText(reveal); } catch { setError('No se pudo copiar. Copiá el token manualmente.'); } }
  return <div className="max-w-4xl"><button type="button" onClick={() => navigate('/teacher')} className="text-sm font-semibold text-gray-500 hover:text-titi-dark mb-4">← Mis cursos</button><h1 className="text-3xl font-black text-titi-dark">Integraciones</h1><p className="text-sm text-gray-500 mt-1 mb-6">Tokens personales para automatizar autoría. El secreto se muestra una sola vez.</p>{error && <div className="bg-red-50 border border-red-200 rounded-xl p-3 mb-4"><p className="text-sm font-semibold text-red-700">{error}</p></div>}{reveal && <div className="bg-titi-cream border-2 border-titi-yellow rounded-2xl p-4 mb-5"><h2 className="text-base font-bold text-titi-dark">Guardá este token ahora</h2><p className="text-xs text-gray-600 mt-1">No queda guardado en navegador y se limpia al cerrar este aviso.</p><code className="block mt-3 bg-white border border-gray-200 rounded-xl p-3 break-all text-xs">{reveal}</code><div className="flex gap-2 mt-3"><button type="button" onClick={copy} className="titi-btn-primary">Copiar</button><button type="button" onClick={() => setReveal(null)} className="titi-btn-ghost">Cerrar y limpiar</button></div></div>}<form onSubmit={create} className="bg-white border border-gray-100 rounded-2xl p-5 flex flex-col gap-4"><h2 className="text-xl font-bold text-titi-dark">Crear token</h2><label className="flex flex-col gap-1.5"><span className="text-sm font-semibold text-titi-dark">Nombre</span><input value={name} onChange={(event) => setName(event.target.value)} maxLength={80} className="titi-input" placeholder="Automatización de mis cursos" /></label><label className="flex flex-col gap-1.5"><span className="text-sm font-semibold text-titi-dark">Expira en horas</span><input type="number" min="1" max="2160" value={expiresInHours} onChange={(event) => setExpiresInHours(event.target.value)} className="titi-input max-w-xs" /></label><fieldset><legend className="text-sm font-semibold text-titi-dark mb-2">Permisos</legend><div className="grid grid-cols-1 sm:grid-cols-2 gap-2">{SCOPES.map((scope) => <label key={scope} className="flex items-center gap-2 text-sm text-titi-dark bg-titi-cream rounded-xl p-2.5"><input type="checkbox" checked={scopes.includes(scope)} onChange={() => toggleScope(scope)} className="accent-titi-yellow" />{scope}</label>)}</div><p className="text-xs text-gray-500 mt-2">El permiso <strong>publish</strong> está desmarcado por defecto.</p></fieldset><button type="submit" disabled={busy} className="titi-btn-primary self-start">{busy ? 'Creando…' : 'Crear token'}</button></form><section className="mt-6"><h2 className="text-xl font-bold text-titi-dark mb-3">Tokens existentes</h2>{loading ? <div className="h-24 bg-white border border-gray-100 rounded-2xl animate-pulse" /> : tokens.length ? <div className="space-y-3">{tokens.map((token) => <article key={token.id} className="bg-white border border-gray-100 rounded-2xl p-4 flex flex-col sm:flex-row sm:items-center gap-3"><div className="flex-1"><p className="font-bold text-titi-dark">{token.nombre} <span className="font-mono text-xs text-gray-500">{token.prefijo}…</span></p><p className="text-xs text-gray-500 mt-1">Expira: {new Date(token.expiresAt).toLocaleString()} · {token.scopes.join(', ')}</p><p className="text-xs font-semibold mt-1 text-gray-500">{token.revokedAt ? 'Revocado' : token.lastUsedAt ? `Último uso: ${new Date(token.lastUsedAt).toLocaleString()}` : 'Sin uso todavía'}</p></div>{!token.revokedAt && <button type="button" onClick={() => revoke(token)} disabled={busy} className="text-red-500 font-bold text-sm">Revocar</button>}</article>)}</div> : <div className="bg-white border border-gray-100 rounded-2xl p-6"><TitiMascot mood="idle" size="sm" message="Todavía no creaste tokens." /></div>}</section></div>;
}
